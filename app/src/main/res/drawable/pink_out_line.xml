<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- or any color you want for the button -->
    <corners android:radius="600dp" />
    <stroke
        android:width="2dp"
        android:color="@color/buttonColor" />
</shape>
